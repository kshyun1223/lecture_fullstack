import areaOfCircle from "./modules/areaOfCircle.js";

const getFromDataSomeWhare = 300; // 임포트한 모듈의 공식에 대입할 값. 리터럴이 아니라 어디선가 연동해서 가져올 수도 있을 것이다. 

// console.log(areaOfCircle(getFromDataSomeWhare));

console.log(areaOfCircle(getFromDataSomeWhare))