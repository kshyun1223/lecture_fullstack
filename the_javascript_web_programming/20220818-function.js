//함수 선언 (declration)
function hairService(parameter) {
  // 실행부분
  console.log('어서오세요' + parameter + '고객님');

  // 돌려주는 부분 
  return "근원씨메롱";
};

// 함수 호출, 할당 (assignment)
hairService("양상희");
console.log('어서오세요' + parameter + '고객님');

// ReferenceError: 참조 에러

// undefined: 정의되지 않았다는 의미의 데이터타입 

// 데이터타입 = function
// console.log(typeof(hairService)); 

